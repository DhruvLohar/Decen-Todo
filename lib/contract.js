import { ethers } from "ethers";
const ContractAddress = "0x8fe1eDbE9E1737c91072D56F9b39cEe8f7235d3e"

export function getContractByProvider(provider) {
    // try {
    //     const { abi } = CreateBlockABI;

    //     const contract = new ethers.Contract(ContractAddress, abi, provider);
    //     return contract;
    // } catch (error) {
    //     console.error(error)
    // }
}

export function getContractBySigner(signer) {
    // try {
    //     const { abi } = CreateBlockABI;

    //     const contract = new ethers.Contract(ContractAddress, abi, signer);
    //     return contract;
    // } catch (error) {
    //     console.error(error)
    // }
}